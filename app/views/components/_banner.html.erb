<div class="banner" style="background-image: linear-gradient(-120deg, rgba(10,20,10,0.3) 50%, rgba(10,20,10,0.2) 100%), url('https://foodrevolution.org/wp-content/uploads/2017/12/blog-featured-holiday_-20171215.jpg');">

  <div class="banner-content">
    <h1 id="first-header">Hungry?</h1> <br>
    <p id="second-header">Find the best lunch deals nearby. <strong> Now!</strong></p>

    <!-- Die Action Methode legt fest, was nach dem Druecken des Submit Buttons passieren soll. In diesem -->
    <!-- Fall soll auf eine neue Seite geleitet werden (restaurant path) -->
    <!-- das Formular bekommt eine Id, das das Plugin dieses aufruft. -->
    <!-- Damit auf der weitergeleiteten Seite die Werte in der URL stehen, braucht es eine name Klasse. -->

    <div class="form-group">
      <form action="<%= restaurants_path %>" class="js-autocomplete-form">
        <input type="text" class="form-control inputfield" value="<%= params[:location] %>" name="location" id="restaurant_location" placeholder="e.g. Berlin">
      </div>
      <div class="form-group banner-spacing">
        <input type="submit" class="form-control find-button" value="FIND DEALS">
      </div>
    </form>
  </div>
</div>
